## Q1. Filter employees with salary > 50,000 and collect their names.

List<String> result = employees.stream()
    .filter(e -> e.getSalary() > 50000)
    .map(Employee::getName)
    .collect(Collectors.toList());

---------------------------------------------------------------------------------------
## Find the highest-paid employee.

String employee = employees.stream().max(Comparator.comparingDouble(Employee::getSalary))

---------------------------------------------------------------------------------------

## Group employees by department.
List<Employees> emp = employees.stream().collect(Collectors.groupingBy(Employees::getDepartment))

---------------------------------------------------------------------------------------

## Find duplicate elements in a List of integers.
Set<Integer> set = new Hashset();
List<Integer> ls = numbers.stream().filter(x -> !set.add(x)).collect(Collectos.toList());
---------------------------------------------------------------------------------------

## Q5. Get top 3 highest-paid employees.
String employee = employees.stream().max(Comparator.comparingDouble(Employee::getSalary)).limit(3)
---------------------------------------------------------------------------------------

## Q6. Count employees in each department.

---------------------------------------------------------------------------------------
## Q7. Increase salary by 10% for employees older than 25.
List<Employee> updated = employees.stream()
    .map(e -> {
        if (e.getAge() > 25) e.setSalary(e.getSalary() * 1.1);
        return e;
    })
    .collect(Collectors.toList());
---------------------------------------------------------------------------------------
## Q8. Find names starting with 'R'.
names.stream.
map(Employees::names).
filter(x -> x.startWith("R")).
collect(Collectors.toList())
---------------------------------------------------------------------------------------

## Q9. Get average salary of all employees.
employees.stream().
maptoDouble(Employees::getSalary).
average();

---------------------------------------------------------------------------------------

## Q10. Separate employees into two groups: age ≤ 25 and age > 25.

Map<Boolean, List<Employee>> partitioned = employees.stream()
    .collect(Collectors.partitioningBy(e -> e.getAge() <= 25));
---------------------------------------------------------------------------------------

## Q11. Find the oldest employee and their department.
Optional<Employee> oldest = employees.stream()
    .max(Comparator.comparingInt(Employee::getAge));
---------------------------------------------------------------------------------------

## Q12. Get a distinct list of department names.
List<String> departments = employees.stream()
    .map(Employee::getDepartment)
    .distinct()
    .collect(Collectors.toList());
---------------------------------------------------------------------------------------
## Q13. Check if any employee earns more than 1 lakh.
boolean anyHighEarner = employees.stream()
    .anyMatch(e -> e.getSalary() > 100000);
---------------------------------------------------------------------------------------
## Q14. Convert a List of strings to uppercase and join with comma.
String result = names.stream()
    .map(String::toUpperCase)
    .collect(Collectors.joining(", "));
---------------------------------------------------------------------------------------
## Q15. Find the second highest salary.

Optional<Double> secondHighest = employees.stream()
    .map(Employee::getSalary)
    .distinct()
    .sorted(Comparator.reverseOrder())
    .skip(1)
    .findFirst();
---------------------------------------------------------------------------------------    